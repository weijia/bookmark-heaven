## Packages
framer-motion | Smooth animations for UI transitions and interactions
date-fns | Formatting dates for bookmarks

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
Authentication uses Replit Auth endpoint /login
Admin features require separate admin authentication flow
